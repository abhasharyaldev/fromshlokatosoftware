// All content on this platform is free. There is no premium tier.
// This file is intentionally minimal — no access gates are needed.

/** Every authenticated user can access every lesson. */
export function canAccessLesson(): true {
  return true
}
